/* Qu'est-ce qu'un pointeur de fonction ?

Que contient un pointeur de fonction ?

L'instruction suivante est-elle un pointeur de fonction : `int *func(int a, float b);` ?

L'instruction suivante est-elle un pointeur de fonction : `int (*func)(int a, float b);` ?

Quelle est la différence entre un pointeur de fonction et une fonction qui renvoie un pointeur ?

Que peut contenir (pointer vers) la variable suivante : `int (*add)(int, int);` ?

Comment affecter la fonction `sum` à lopération du pointeur de fonction ? */

int sum(int a, int b) {
    return a + b;
}
int main() {
    int (operation)(int, int);

		return (0);
}

/* 8.Comment appeler la fonction de multiplication via une opération de pointeur de fonction  */

int multiply(int a, int b) {
    return a b;
}

int main() {
    int (operation)(int, int);
    operation = multiply;
  
		
    return (0);


/* Qu'est-ce qu'une constante (`const`) en C ?

Quel est le type du pointeur suivant : `int *const ptr;` ?

Quel est le type du pointeur suivant : `const int* ptr;` ?

Quel est le type du pointeur suivant : `const int* const ptr;` ?

Qu'est-ce qu'une fonction variadique ?

Comment déclarer une fonction variadique en C ?

Comment accéder aux variables d'une fonction variadique ? */
999999999
999999909
999999009
999990099
999900999
999009999
990099999
910999999
910999999
990099999
999009999
999900999
999990099
999999009
999999909
